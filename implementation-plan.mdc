# Implementation plan — relath01

## Step 1 — Repository & schema foundation

Done

Created monorepo layout under `cursor_project_rules`, `neo4j/cypher`, `backend`, `web`, `h5`, `mcp-server`, `docker`, and `docker-compose.yml` for single-host orchestration.

Neo4j constraints/indexes and node/edge property conventions are encoded in `neo4j/cypher`; Spring Boot API and frontends are scaffolded for iterative feature work.

## Step 2 — Neo4j rule engine & core API

Done

Parameterized Cypher catalog (`CypherQueries`), `GraphAnalyticsService`, public graph REST (`/api/v1/public/graph/**`), `ApiExceptionHandler`, slice tests, MCP `relath_graph_summary` with fetch error handling; Compose waits for Neo4j before starting backend.

## Step 3 — RBAC, MySQL metadata, audit

Pending

## Step 4 — Web/H5 visualization & reports

In progress

Web 管理端：概览含示例 JSON 导入（批次一/二、一键导入）、工具页默认样例 ID；后端 `batch-ingest` 写入 Neo4j。H5 与报告模板仍待办。

## Step 5 — Multimodal ingest & Skills

Pending

## Step 6 — Hardening & ops

Pending
