spring:
  application:
    name: relath-backend
  neo4j:
    uri: bolt://localhost:7687
    authentication:
      username: neo4j
      password: relath_dev_password

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info
